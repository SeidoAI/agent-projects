"""Development / prompt-engineering tooling commands.

Click subcommands organized as developer / prompt-tooling utilities;
each module registers itself with ``cli.add_command`` in ``main.py``.
The ``_dev/`` prefix is internal organization, not part of the
user-facing CLI surface.
"""
